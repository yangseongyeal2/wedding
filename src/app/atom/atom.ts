import { atom } from 'jotai';



export const floatingButtonOpenAtom = atom<boolean>(false);
export const showModalMoneyAtom = atom<boolean>(false);
export const showRSVPModalMoneyAtom = atom<boolean>(false);
export const isLoadingAtom = atom<boolean>(false);